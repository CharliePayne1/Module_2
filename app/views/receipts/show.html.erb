<h3> <%= @receipt.customer_name %>'s transaction with <%= @receipt.retailer_name %> </h3>
<table>
        <thead>
          <tr>
            <th>Transaction ID</th>
                <th>Total</th>
              <th>Date</th>
              <th>Items Purchased</th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td><%= @receipt.id %></td>
            <td> £<%= @receipt.total_amount.round(2) %></td>
            <td><%= @receipt.created_at %></td>
            <td><%= @receipt.items %></td><br>
          </tr>
        </tbody>
</table>
<br>
<br>

  <button>
  <%= link_to "Show #{@receipt.customer_name}", customer_path(@receipt.bank_account.customer)  %> 
 </button>

   <button>
  <%= link_to "Show #{@receipt.account_type} Account", bank_account_path(@receipt.bank_account_id)  %> 
 </button>

   <button>
  <%= link_to "Show Retailer", retailer_path(@receipt.retailer_id)   %> 
 </button>
